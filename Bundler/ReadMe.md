we use bundler for production ready code.
the bundler name are 1.parcel,2.vite,3.webpack

we will use parcel

steps
1.npm init. (it initialize the project).
2.npm install.  (it simply tells the requirement of project include information of project).
3.npm install parcel.(from this command we included parcel bundler in our project for production ready code).
4.npm install react. (this command is use to install react in my project).
5.npm install react-dom. (this command is use to install react-dom in my project).