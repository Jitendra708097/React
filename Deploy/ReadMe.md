Deploy our React project 

initiliaze using npm init command.

for creating a server for deployment we use this command (npx parcel filename.html).

We build production ready code using this command (npx parcel build filename.html).

In this section we started JSX(Java script XML).
it use for write HTML like code in direct Java script.



The conversion of JSX format into React.createElement()i.e. in react which is done by babel.
Babel is also known as a transpiler.

JXS wants only single element so that we use wrapper (<>...</>).

JSX we can conclude Java Script expresion so that we use {java script expresion}.

In JSX If we want add inline css then we use style attribute in it expect java script object.

syntax : style{{write css in this}}.

In this lecture we starting to learn react component.

There are two types of react component.
1.class base component.  it is outdated method so it not in use right now.
2.function base component.  we conitnue from this.